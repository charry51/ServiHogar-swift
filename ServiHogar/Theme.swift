import SwiftUI

struct Theme {
    static let azulTexto = Color(red: 0, green: 0.38, blue: 0.66)
    static let azulBoton = Color(red: 0, green: 0.38, blue: 0.66)
    static let fondoPrincipal = "FondoPrincipal"
    static let fondoLogin = "FondoLogin"
    
    // Corner Radii
    static let cornerRadiusBoton: CGFloat = 25
    static let cornerRadiusTarjeta: CGFloat = 15
    static let cornerRadiusCampo: CGFloat = 10
    
    // Spacing
    static let paddingHorizontal: CGFloat = 20
    static let spacingElementos: CGFloat = 15
}
